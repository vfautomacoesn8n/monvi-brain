# Inbox de tarefas

Recebe novas tarefas ainda não triadas. Use o [template de tarefa](../../templates/task.md); defina escopo e responsável antes de mover para `active`. Declare separadamente `allowed_paths` para alteração, `read_only_paths` para consulta e `forbidden_paths` para bloqueio total; os conjuntos não podem se sobrepor.
